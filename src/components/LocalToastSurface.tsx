'use client';

/**
 * @deprecated This component is now a no-op.
 *
 * Toast, confirm, and prompt surfaces are rendered by NotificationProvider
 * (mounted in app/[locale]/layout.tsx). This stub remains so existing
 * imports don't break during the transition.
 */
export function LocalToastSurface() {
  return null;
}
